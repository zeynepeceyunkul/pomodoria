# API base URL — use your LAN IP for Expo Go on a physical device
# Run: npm run setup:api  (writes this automatically on Windows)
EXPO_PUBLIC_API_URL=http://192.168.1.100:5000

# Optional: web app URL for parsing email links (defaults to localhost:5173)
EXPO_PUBLIC_APP_URL=http://localhost:5173
